org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Proyek ada di drive D: sedangkan cache paket Flutter di drive C:. Kompilasi
# Kotlin inkremental gagal bila sumber dan hasil build beda drive ("Could not
# close incremental caches"), jadi dimatikan.
kotlin.incremental=false
# Plugin tflite_flutter 0.12.1 menyetel Java 11 tetapi tidak menyetel target
# Kotlin (default 17). Aman untuk Android; ubah error validasinya jadi peringatan.
kotlin.jvm.target.validation.mode=warning
